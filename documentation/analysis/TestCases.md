| Test Case ID | Test Case Name | Description | Precondition | Steps | Expected Result | Actual Result |
|---|---|---|---|---|---|---|
| TC_UC_01a | Log In – Success | Verify a user with a valid Lichess account can log in via OAuth PKCE. | User has a Lichess account. User is on the `/login` page. | 1. User opens `/login`.<br>2. User clicks **"Login with Lichess"**.<br>3. Browser generates a PKCE code verifier and challenge, stores verifier in `sessionStorage`.<br>4. Browser redirects to `https://lichess.org/oauth` with `client_id=chess-assistance`, `scope=board:play`, `code_challenge_method=S256`.<br>5. User authorises the app on Lichess.<br>6. Lichess redirects to `/callback?code=...`.<br>7. Callback page sends `POST /auth/lichess/callback` with `{code, code_verifier}`.<br>8. API Gateway exchanges code for Lichess access token, fetches account via `GET https://lichess.org/api/account`, registers or updates player in DB via `RegisterPlayer` gRPC, sets httpOnly cookie `chess_session` (JWT, 24 h), returns `{player_username, player_id}`.<br>9. Callback page stores `lichess_user` in `localStorage`, then redirects to `/` after 1 500 ms. | Callback page shows **"Successfully logged into Lichess."** followed by **"Logged in as: {username}"**. After 1.5 s the user is redirected to the Home page (`/`). Navbar knight-icon dropdown shows **"{username}"** and **"Logout"**. | Callback page shows "Successfully logged into Lichess." and "Logged in as: {username}". User is redirected to `/` after 1 500 ms. `lichess_user` saved in localStorage. Navbar shows the Lichess username and Logout option. |
| TC_UC_01b | Log In – Denied on Lichess | Verify login fails gracefully when the user denies authorisation on Lichess. | User is on `/login`. | 1. User clicks **"Login with Lichess"**.<br>2. Lichess OAuth page opens.<br>3. User clicks "Cancel" / denies the app.<br>4. Lichess redirects to `/callback?error=access_denied`. | Callback page shows **"Lichess login failed: {error}"** where `{error}` is the value of the `error` query parameter returned by Lichess. No redirect to Home. **"Go back home"** button is visible. | Callback page displays "Lichess login failed: access_denied". User stays on the callback page. |
| TC_UC_01c | Log In – API Gateway Unreachable | Verify error feedback when the API Gateway is not running. | User is on `/login`. API Gateway is not running. | 1. User clicks **"Login with Lichess"** and completes Lichess OAuth.<br>2. Callback page tries to `POST /auth/lichess/callback`.<br>3. `fetch()` throws a `TypeError` (network error). | Callback page shows **"Cannot reach the API gateway at http://localhost:3001 — is it running?"** | Callback page shows "Cannot reach the API gateway at http://localhost:3001 — is it running?" |
| TC_UC_02a | Log Out – Success | Verify a logged-in user can log out and the session is cleared. | User is logged in. No active chess session. | 1. User clicks the knight icon in the navbar.<br>2. Dropdown shows **"{username}"**, **"IoT Dashboard"**, **"Player Preference"**, **"Logout"**.<br>3. User clicks **"Logout"**.<br>4. Frontend sends `POST /auth/lichess/logout` with `credentials: include`.<br>5. API Gateway revokes Lichess token via `DELETE https://lichess.org/api/token`, clears `chess_session` cookie, returns `{success: true}`.<br>6. Frontend removes `lichess_user` from `localStorage`. | Navbar dropdown no longer shows username or Logout. Dropdown now shows **"Login"** link. `lichess_user` removed from `localStorage`. `chess_session` cookie cleared. | Navbar shows "Login" link. localStorage and cookie cleared. `onLogout` callback resets Home page state (sessionId cleared, monitoring stopped). |
| TC_UC_02b | Log Out – With Active Session | Verify an active chess session is ended automatically when logging out. | User is logged in with an active chess session (session ID stored in `localStorage` as `active_session_id`). | 1. User clicks **"Logout"**.<br>2. Frontend detects `active_session_id` in `localStorage`.<br>3. Frontend fires `navigator.sendBeacon` to `POST /session/end` with `{session_id}`.<br>4. Then calls `POST /auth/lichess/logout`. | Session is ended on the server before logout completes. `active_session_id` removed from `localStorage`. User is logged out. | Session is ended via sendBeacon, then logout is processed. Both `active_session_id` and `lichess_user` are removed from localStorage. |
| TC_UC_03a | Start Session – No Readiness Alerts | Verify a session starts immediately when the user has adequate sleep and hydration. | User is logged in. Home page monitoring is started. No active session exists. | 1. User clicks **"Start Chess Session"** button.<br>2. Sleep form appears with: **"Time you went to sleep"** (time picker), **"Time you woke up"** (time picker), **"Water intake so far (ml)"** (number input, step 100).<br>3. User enters values yielding ≥ 7 h of sleep, woke up ≥ 2 h ago, sufficient water.<br>4. User clicks **"Start Session"**.<br>5. Frontend sends `POST /session/evaluate` with `{sleep_time, awaken_time, water_intake_ml}`.<br>6. `evaluateReadiness` returns `{alerts: [], sleep_duration, awake_duration}`.<br>7. Because `alerts` is empty, `startSession()` is called automatically.<br>8. Frontend sends `POST /session/start` with `{sleep_time, awaken_time, confirmed_at, water_intake_initial_ml}`.<br>9. LichessApiService creates a `HealthRecord` and `Session` in DB, starts Lichess stream, returns `{session_id, success: true, message: "Session started, listening for matches"}`. | No alert screen is shown. Home page immediately shows **"Session #{id} is active"** with an **"End Session"** button. `active_session_id`, `session_minutes_slept`, `session_minutes_awake` saved to `localStorage`. | Session starts without alert screen. "Session #{id} is active" appears with "End Session" button. active_session_id, session_minutes_slept, session_minutes_awake saved to localStorage. |
| TC_UC_03b | Start Session – Red Alert (< 5 h Sleep) | Verify a red readiness alert is shown for critically low sleep. | User is logged in, monitoring started, no active session. | 1. User opens sleep form and enters sleep/wake times resulting in less than 5 hours of sleep.<br>2. User clicks **"Start Session"**.<br>3. `POST /session/evaluate` returns one alert with `level: "red"`. | **"Session Readiness Check"** screen appears showing sleep and awake durations. Red alert with message: **"You only slept Xh Ym. Consider resting first."** Buttons: **"Start Anyway"** and **"Cancel"**. Session is NOT started automatically. | "Session Readiness Check" shown with red "You only slept Xh Ym. Consider resting first." and "Start Anyway" / "Cancel" buttons. |
| TC_UC_03c | Start Session – Yellow Alert (5–7 h Sleep) | Verify a yellow readiness alert is shown for sub-optimal sleep. | User is logged in, monitoring started, no active session. | 1. User enters sleep/wake times yielding between 5 and 7 hours of sleep.<br>2. User clicks **"Start Session"**.<br>3. `POST /session/evaluate` returns one alert with `level: "yellow"`. | **"Session Readiness Check"** shown. Yellow alert with message: **"You slept Xh Ym — not ideal but playable."** Buttons: **"Start Anyway"** and **"Cancel"**. | "Session Readiness Check" shown with yellow "You slept Xh Ym — not ideal but playable." and "Start Anyway" / "Cancel" buttons. |
| TC_UC_03d | Start Session – Yellow Alert (Just Woke Up) | Verify alert when user woke up less than 30 minutes ago. | User is logged in, monitoring started, no active session. | 1. User enters wake time that is less than 30 minutes in the past.<br>2. User clicks **"Start Session"**. | Yellow alert: **"You just woke up — give yourself time to fully wake up."** | "You just woke up — give yourself time to fully wake up." shown as yellow alert. |
| TC_UC_03e | Start Session – Already Has Active Session | Verify the system recovers gracefully when an active session already exists in the DB. | User is logged in but already has an active session in the DB. | 1. User opens sleep form and clicks **"Start Session"**.<br>2. `POST /session/start` is called.<br>3. LichessApiService returns `{success: false, message: "Player already has an active session (id: 5)"}`. | Frontend detects the existing session ID from the message via regex `active session \(id:\s*(\d+)\)`, sets `sessionId` to the existing ID, saves it to `localStorage`, and shows **"Session #5 is active"** with **"End Session"** button. | Existing session is detected from the message. "Session #{id} is active" displayed without creating a new session. |
| TC_UC_04a | End Session – Success | Verify a user can end an active chess session. | User is logged in with an active session shown as **"Session #{id} is active"**. | 1. User clicks **"End Session"** button.<br>2. Button text changes to **"Ending..."**.<br>3. Frontend sends `POST /session/end` with `{session_id}`.<br>4. LichessApiService sets `session.EndedAt = UtcNow`, returns `{success: true, message: "Session ended"}`. | "Session #{id} is active" panel disappears. **"Start Chess Session"** button reappears. `active_session_id` removed from `localStorage`. | "Session ended" from backend. Home page shows "Start Chess Session" button again. active_session_id cleared from localStorage. |
| TC_UC_05a | View Dashboard – Live Metrics Displayed | Verify all sensor cards appear with real-time data when monitoring is started. | User is on the Home page (`/`). IoT service is running with sensor readings available. | 1. User clicks **"Start Monitoring"** on the hero section.<br>2. Dashboard section appears below the hero.<br>3. Frontend immediately fetches `GET /iot/temp?id=1`, `GET /iot/light?id=1`, `GET /iot/co2?id=1` in parallel.<br>4. Values are displayed in the **"Live Metrics"** grid.<br>5. Fetch repeats every **10 seconds** automatically. | **"Live Metrics"** section shows four cards: **Temperature** (e.g. "22.3°C"), **Light Level** (e.g. "450.0 lux"), **CO2 Level** (e.g. "850.0 ppm"), **Water Drank** ("0 ml" with "+" button). **"Session Performance"** card shows hardcoded values: Games **5**, Wins **3**, Losses **2**, Win Rate **60%**. | All four cards display. Values formatted to 1 decimal place. Session Performance stats are hardcoded in code, not fetched from DB. Auto-refresh every 10 s. |
| TC_UC_05b | View Dashboard – Sensor Data Unavailable | Verify the dashboard shows a fallback when sensor data is missing or the request fails. | IoT service is unreachable or state store has no data. | 1. User clicks **"Start Monitoring"**.<br>2. `fetch()` calls fail or return `null` value fields. | Cards show **"—"** for Temperature, Light Level, and CO2 Level. No error message shown to user (error only logged to console). | Cards show "—" for all three sensor values. Errors caught silently without showing a message to the user. |
| TC_UC_06a | IoT Dashboard – Data Loaded | Verify the dedicated IoT Dashboard page displays sensor data and comfort status. | User navigates to `/iot-dashboard`. IoT service is running. | 1. Page loads and fetches all three sensor endpoints.<br>2. Sensor cards appear in **"Sensor Summary"** section.<br>3. "Connected Device Health" card shows `success` and `message` from the temperature response.<br>4. "IoT Response JSON" section shows the raw JSON payload. | **Temperature** (e.g. "22.3°C"), **Light Level** (e.g. "450.0 lux"), **CO2 Level** (e.g. "850.0 ppm"), **Environment Status** derived from temperature. Success: **"Yes"**. Message: **"Latest temperature reading for Arduino 1 retrieved successfully."** Full raw JSON visible. | All cards populated. Environment Status computed from temperature. Raw JSON payload shown in pre block. |
| TC_UC_06b | IoT Dashboard – Comfort Status: Cool | Verify the comfort status label for temperature below 18 °C. | IoT service reports temperature < 18 °C. | 1. Navigate to `/iot-dashboard`.<br>2. Temperature value is less than 18.0. | **Environment Status** card shows **"Cool"**. | "Cool" displayed. |
| TC_UC_06c | IoT Dashboard – Comfort Status: Comfortable | Verify the comfort status label for temperature between 18 and 26 °C. | IoT service reports temperature in range 18–26 °C. | 1. Navigate to `/iot-dashboard`.<br>2. Temperature value is between 18.0 and 26.0 (inclusive). | **Environment Status** card shows **"Comfortable"**. | "Comfortable" displayed. |
| TC_UC_06d | IoT Dashboard – Comfort Status: Warm | Verify the comfort status label for temperature above 26 °C. | IoT service reports temperature > 26 °C. | 1. Navigate to `/iot-dashboard`.<br>2. Temperature value is greater than 26.0. | **Environment Status** card shows **"Warm"**. | "Warm" displayed. |
| TC_UC_06e | IoT Dashboard – Fetch Error | Verify the dashboard displays an error alert when the temperature fetch fails. | IoT service is unreachable. | 1. User navigates to `/iot-dashboard`.<br>2. `GET /iot/temp?id=1` returns non-2xx or `data.error` is set. | Red alert banner with the error message (from `data.error` or **"Unable to fetch IoT data"**). All sensor cards show **"N/A"**. **"Refresh Data"** button remains visible. | Error alert shown. Cards display "N/A". |
| TC_UC_07a | Log Water Intake – Add Amount | Verify a user can log water drunk during a session. | User is on the Home page with monitoring started. | 1. User sees **"Water Drank: 0 ml"** card with a **"+"** button.<br>2. User clicks **"+"**.<br>3. Number input (step 50) and buttons **✓** (confirm) and **✕** (cancel) appear.<br>4. User types `250` in the input.<br>5. User clicks **✓**. | Water Drank card updates to **"250 ml"**. Input field disappears and **"+"** button reappears. | Total water updated in local React state only. NOT sent to backend. Resets to 0 on page refresh. |
| TC_UC_07b | Log Water Intake – Cancel | Verify cancelling water input leaves the total unchanged. | User is on the Home page with monitoring started. | 1. User clicks **"+"** on the Water Drank card.<br>2. User types any value.<br>3. User clicks **✕**. | Water Drank total remains unchanged. Input field disappears and **"+"** button reappears. | Total water unchanged. Input hidden. State reverts. |
| TC_UC_08a | Chess.com Lookup – Success | Verify Chess.com player statistics are retrieved and displayed. | User is on `/chesstrack`. | 1. User enters a valid Chess.com username (e.g. `hikaru`) in the **"Chess.com Player Lookup"** section.<br>2. User clicks **"Search Player"** (button shows **"Searching..."**).<br>3. System fetches `https://api.chess.com/pub/player/{username}/stats`. | Four metric cards under **"{username} Stats"**: **Rapid Rating** + Win Rate %, **Blitz Rating** + Win Rate %, **Bullet Rating** + Win Rate %, **Total Rapid Games** (W / L / D). Summary card shows Rapid WR, Blitz WR, Bullet WR, Best Rating (max of the three). | All four metric cards populated. Win rates calculated as wins divided by total games, rounded to the nearest percent. |
| TC_UC_08b | Chess.com Lookup – Player Not Found | Verify error shown for a non-existent Chess.com username. | User is on `/chesstrack`. | 1. User enters a username that does not exist on Chess.com.<br>2. Clicks **"Search Player"**.<br>3. API returns non-2xx response. | Error message in red: **"Could not find this Chess.com player."** | "Could not find this Chess.com player." shown. No cards displayed. |
| TC_UC_08c | Chess.com Lookup – Empty Username | Verify client-side validation for empty username. | User is on `/chesstrack`. | 1. User leaves the Chess.com username field empty.<br>2. Submits the form. | Error message: **"Please enter a Chess.com username."** No API call is made. | "Please enter a Chess.com username." shown immediately without network request. |
| TC_UC_09a | Lichess Lookup – Success | Verify Lichess player statistics are retrieved and displayed. | User is on `/chesstrack`. | 1. User enters a valid Lichess username in the **"Lichess Player Lookup"** section.<br>2. User clicks **"Search Player"**.<br>3. System fetches `https://lichess.org/api/user/{username}`. | Four metric cards under **"{username} Stats"**: **Rapid Rating** + Games count, **Blitz Rating** + Games count, **Bullet Rating** + Games count, **Classical Rating** + Games count. Summary card shows: Puzzle rating, Storm score, Patron (Yes/No), Online (Yes/No). | All four metric cards populated. Summary stats from `perfs` and top-level fields of Lichess API response. |
| TC_UC_09b | Lichess Lookup – Player Not Found | Verify error for a non-existent Lichess username. | User is on `/chesstrack`. | 1. User enters a username that does not exist on Lichess.<br>2. Clicks **"Search Player"**. | Error message in red: **"Could not find this Lichess player."** | "Could not find this Lichess player." shown. |
| TC_UC_10a | Tilt Predictor – Successful Prediction | Verify angriness level is predicted and displayed for an analyzed Lichess game. | User is on `/angriness`. The game has engine analysis available on Lichess. | 1. User enters a valid Lichess game ID and player username.<br>2. User clicks **"Predict"** (button shows **"Predicting..."**).<br>3. Frontend sends `POST {ML_API_URL}/angriness/predict-by-game-id` with `{game_id, player_username}`.<br>4. ML service fetches game from Lichess, finds player side, computes features from analysis, runs model. | Angriness scale (1–5) with 5 colour-coded segments. Active segment highlighted (green=1, lime=2, yellow=3, orange=4, red=5). Four metric cards: **Angriness Level** (e.g. "3/5" + label **"Moderate"**), **Player Rating** vs opponent rating, **Opening** name, **Time Control** + player side (e.g. "white pieces"). **"View game on Lichess"** link shown. | Prediction displayed with scale and cards. Labels: 1=Very Calm, 2=Calm, 3=Moderate, 4=Tilted, 5=Very Tilted. |
| TC_UC_10b | Tilt Predictor – Analysis Required | Verify feedback when game has no computer analysis. | User is on `/angriness`. The game has NOT been analyzed by Lichess. | 1. User enters a game ID without engine analysis and player username.<br>2. Clicks **"Predict"**.<br>3. ML service returns `{status: "analysis_required", game_id, game_url, message}`. | **"Game analysis required"** section appears with message: **"This game has not been analyzed yet. Please visit the game on Lichess and click 'Request a computer analysis', then try again."** Controls: **"Open game on Lichess"** link and **"Retry"** button. | Analysis required section shown with the exact message from ML API. |
| TC_UC_10c | Tilt Predictor – Empty Fields Validation | Verify validation when game ID or username is missing. | User is on `/angriness`. | 1. User leaves one or both input fields empty.<br>2. Clicks **"Predict"**. | Error message: **"Please enter both a game ID and player username."** No API call is made. | "Please enter both a game ID and player username." shown. |
| TC_UC_10d | Tilt Predictor – Recent Games List | Verify a user's recent Lichess games are fetched and listed. | User is on `/angriness`. | 1. User enters a Lichess username in **"Or pick from recent games"** and clicks **"Search"** (or clicks **"My Games"** if logged in).<br>2. Frontend sends `GET {ML_API_URL}/angriness/recent-games/{username}`.<br>3. ML service fetches last 15 games from Lichess and returns up to 10. | Up to 10 games listed, each showing: opening name, opponent, result (green=win, red=loss, yellow=draw), time control, speed. Orange badge **"{N} loss(es) before"** if consecutive losses > 0. Yellow **"No analysis"** badge if unanalyzed. **"Predict"** button per game. | Up to 10 games displayed with correct colours and badges. "My Games" button only visible when logged in. |
| TC_UC_11a | Environment Recommendation – Improvement Found | Verify a recommendation is generated when adjusting an environmental factor improves predicted win probability. | User is on `/recommend-environment`. IoT service is running. | 1. Page auto-fills form from IoT sensors: **Temperature**, **CO2**, **Light** (all read-only). **Minutes Slept** and **Minutes Awake** pre-filled from `localStorage` if a session was started.<br>2. User clicks **"Get Recommendation"** (shows **"Checking..."**).<br>3. Frontend sends `POST {ML_API_URL}/recommend-environment` with `{minutes_slept, minutes_awake, temperature_celsius, co2, light}`.<br>4. ML API tests: temperature→20°C, CO2→500 ppm, light→1500 lux; returns factor with highest positive gain. | **"Recommended Action"** section: factor heading (**"Temperature"**, **"CO2"**, or **"Light"**), message **"Changing {factor} from {current} to {recommended} may increase your win probability by {X} percentage points."**, stats (Current / Improved / Increase in pp). Four cards: Factor, Current Value (with unit), Recommended Value (with unit), Probability Gain. **"All Candidates"** list shows all three factors. | Recommendation displayed. Units: Temperature in C, CO2 in ppm, Light in lux. |
| TC_UC_11b | Environment Recommendation – No Improvement Found | Verify message when no environmental factor improves the prediction. | Environment values already at or near optimal (temp ≈ 20°C, CO2 ≈ 500 ppm, light ≈ 1500 lux). | 1. Page pre-fills with optimal sensor values.<br>2. User clicks **"Get Recommendation"**.<br>3. ML API returns no candidate with positive gain. | **"Recommended Action"** heading shows **"No change recommended"**. Message: **"No environmental improvement increased the prediction according to the model."** "All Candidates" list still shows all three factors. | "No change recommended" heading and no-improvement message from ML API shown. |
| TC_UC_11c | Environment Recommendation – ML API Unreachable | Verify error message when the ML service is unavailable. | ML API is not running. | 1. User clicks **"Get Recommendation"**.<br>2. `fetch()` throws a network error. | Error message in red: **"Could not connect to the ML API."** | "Could not connect to the ML API." shown. |
| TC_UC_12a | Player Preferences – Save (UI Only) | Verify the preferences form submits and shows a success message with no backend persistence. | User navigates to `/preferences`. | 1. User fills in: id, **Daily Game Limit**, **Daily Playtime Limit (min)**, **Break Interval (min)**, **Recommended Rest (min)**.<br>2. User clicks **"Save Preferences"**. | Green success alert: **"Preferences saved successfully!"** — disappears after **3 seconds**. | "Preferences saved successfully!" shown for 3 s. No API call is made. Preferences are NOT persisted — data is lost on page refresh. |
| TC_UC_13 | Open Window | Verify the system can send an "open window" command to the IoT device. | IoT device with window actuator connected. | 1. Navigate to a page with an open-window control.<br>2. Click the open-window button. | System sends a command to the IoT device to open the window. Confirmation displayed. | **Not implemented.** No UI or actuator command exists in the frontend, API Gateway, or IoT service. No window control method is defined in the IoT service. |
| TC_UC_14 | Adjust Light | Verify the system can adjust the room's light intensity. | IoT device with light actuator connected. | 1. Navigate to a page with a light control.<br>2. Set desired light level and confirm. | System sends a command to adjust the light. New level confirmed. | **Not implemented.** No light adjustment UI or actuator command exists. The light endpoint only reads sensor data and cannot control the device. |
| TC_UC_15 | Playtime Alert | Verify the system alerts the user when they exceed their daily playtime limit. | Player preferences include a Daily Playtime Limit. Active session running. | 1. Player plays chess beyond the configured playtime limit.<br>2. System detects the limit exceeded. | Alert shown to the user warning them to stop playing. | **Not implemented.** The playtime limit field exists in the preferences form but preferences are never saved to the backend. No monitoring logic tracks session duration against any limit. |
| TC_UC_16 | View Physical Condition Status and Advice | Verify the system displays a comprehensive physical condition assessment. | User has logged health data. | 1. Navigate to the physical condition page.<br>2. View condition status and advice. | System shows sleep quality, hydration level, and personalised advice. | **Not implemented.** No dedicated physical condition page or endpoint exists. Only the pre-session readiness check (`POST /session/evaluate`) provides brief sleep/water alerts. |
| TC_UC_17 | View Activity Data (Overview) | Verify the system shows historical activity data across sessions. | Player has completed at least one session. | 1. Navigate to the activity overview page or section. | Historical summaries of games played, wins/losses, and session data shown. | **Not implemented.** The "Session Performance" card on the Home page is hardcoded (Games: 5, Wins: 3, Losses: 2, Win Rate: 60%) and not fetched from the database. No activity history page or API endpoint exists. |
